"""Metadata filter strategy package — query-side pre-filter for hybrid_search.

Public surface is :func:`build_metadata_filter` (in :mod:`.registry`).
Concrete strategies live in sibling modules and are registered there.
"""
