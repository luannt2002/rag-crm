# REMAINING FLOWS — INVENTORY 4 luồng cuối (2026-06-26)

> Tổng hợp 4 sweep READ-ONLY: conversation/multi-turn · document-lifecycle · retrieval-embedding-QUALITY · observability-errors.
> Mindset CLAUDE.md: evidence `file:line`/DB-row, nhãn SỰ THẬT vs GIẢ THUYẾT, EVOLVE-không-rewrite, N+1 (bot mới 0-code). **CHƯA chạy load-test multi-turn/retrieval → severity dựa code+DB evidence (BASELINE TĨNH), chưa VERIFIED runtime.**

---

## 1. VẤN ĐỀ MỚI (P0 trước, rồi P1, P2)

| id | tên | luồng | sev | evidence | nhãn | N+1-impact | tiêu-chí vi phạm | fix-hint |
|---|---|---|---|---|---|---|---|---|
| **MT-1** | Hai kho lịch sử multi-turn TÁCH RỜI theo entrypoint — `chat_histories` (HTTP/SSE) vs `messages`/`conversations` (worker queue) | conv/multi-turn | **P0** | chat_stream.py:163 + chat_routes.py:322 đọc từ `chat_histories` (key connect_id); chat_stream.py:417 `INSERT INTO chat_histories`; grep `INTO chat_histories` = chỉ 2 file HTTP, 0 hit worker. Worker: pipeline.py:483 đọc qua `conv_repo.get_by_id`; pipeline.py:286-295 ghi `message_repo.create`; conversation_repository.py:248-272 đọc bảng `messages`. Cùng bot+connect_id: T1 qua SSE, T2 qua worker → T2 KHÔNG thấy T1 | SỰ THẬT | Bot mới KHÔNG có config hợp nhất — 2-store hard-wired. Vi phạm S2 + S3 | S2 (0-config không fix), S3 (history-store không qua 1 Port), HEADLESS-BE canonical-flow | Hợp nhất về 1 `ConversationHistoryPort` (DDD aggregate `messages`/`conversations`) cho mọi entrypoint; đặt HTTP route đọc/ghi qua `conv_repo` như worker |
| **DLC-1** | Header-idempotency row không bao giờ rời `processing` — `mark_done`/`mark_failed` là dead code | doc-lifecycle | **P1** | ingest_idempotency_service.py:227/:255 định nghĩa nhưng grep src/ = 0 call-site prod (chỉ test gọi). Prod chỉ `check_and_record` (documents.py:134) INSERT status='processing', record_document_id=None. bootstrap.py:794-795 comment claim worker gọi nhưng document_worker.py không import. Replay trong TTL → status='processing' + doc_id='' mãi | SỰ THẬT | Bot mới dùng `X-Idempotency-Key` retry → replay 'processing' vĩnh viễn, doc_id rỗng. 0-code KHÔNG fix (wiring thiếu toàn cục) | S5 fail-loud / wiring-completeness (contract service.py:22 'Worker calls mark_done/mark_failed' chưa implement) | Wire `mark_done` ở success-path worker (~line 600), `mark_failed` ở except (~line 630); thread idempotency_key qua DocumentUploaded payload |
| **DLC-2** | Doc `state='failed'` kẹt vĩnh viễn — recovery sweep không bao giờ re-process | doc-lifecycle | **P1** | ingest_stages_store.py:455/:484 UPDATE state='failed' khi embed fail; finalize ingest_stages_final.py:294 'failed' khi coverage<floor. Nhưng document_recovery_worker.py:174-197 WHERE chỉ recover ('DRAFT' aged) OR ('active' + 0 chunks). grep = 0 reprocessor cho 'failed'. Transient 429/CB-open → dark vĩnh viễn | SỰ THẬT | Bất kỳ bot gặp transient embed error (ZE 429, CB OPEN) → doc fail vĩnh viễn, cần re-upload tay. Không config nào recover | S5 fail-loud + graceful-degradation (recovery có lỗ: transient nên retry, terminal nên giữ — code không phân biệt, retry cả 2 = không) | Thêm branch recovery cho state='failed' AND aged AND retry-count<N; reuse anti-dup outbox join |
| **MT-2** | Heuristic intent fast-path BYPASS condense — follow-up phụ thuộc ngữ cảnh không được fold history | conv/multi-turn | **P1** | understand.py:116-142: nếu heuristic_intent_enabled (DEFAULT=True) + confidence>=threshold → `return` NGAY, trước condense ở :157-176. heuristic_intent_classifier.py:126-129 multi_hop/comparison/aggregation = 0.85; THRESHOLD=0.85 → fire. i18n.py:283-291 pattern mid-string KHÔNG anchored. Follow-up 'vì sao vậy?' khớp → retrieve query thô chưa condense | SỰ THẬT | Mọi bot dùng locale có intent_patterns mid-string (vi seed sẵn). Bot mới không sửa code vẫn dính — silent coverage loss. T1-Smartness drop | S4 (history-aware retrieval phải chạy TRƯỚC intent shortcut), T1-Smartness (Coverage drop follow-up) | Skip heuristic khi `has_meaningful_history` (cùng điều kiện understand.py:158-163), hoặc condense TRƯỚC heuristic, hoặc nâng threshold mid-string >0.85 |
| **MT-3** | Rolling-summary ASYMMETRY + hardcode tiếng Việt inject vào history (HTTP có, worker không) | conv/multi-turn | **P1** | _shared.py:247-279 `_apply_rolling_summary` gọi ở chat_routes.py:337 (HTTP sync); chat_stream.py + pipeline.py KHÔNG gọi (grep `rolling` pipeline.py=0; pipeline.py:490 raw last-N). _shared.py:276 HARDCODE 'Tóm tắt hội thoại trước ({N} tin nhắn)' — APP tự chế system-message bơm vào prompt | SỰ THẬT | Bot non-VI: history bị chèn tiếng Việt khi qua HTTP-sync, không khi SSE/worker — không config được | Domain-neutral TUYỆT ĐỐI (VN literal _shared.py:276), S2, Quality-Gate#8, nhất quán cross-entrypoint | Rolling-summary text từ `language_packs[locale]` (DB-driven); apply ở 1 chỗ chung (cùng tầng history-load Port) cho 3 entrypoint đồng nhất |
| **MT-4** | KHÔNG serialize per-conversation — turn đồng thời cùng connect_id race history-load + ordering | conv/multi-turn | **P1** | pipeline.py:728 `Semaphore(concurrency)` + :739 cho phép N turn song song, 0 lock per-conv (grep `FOR UPDATE\|with_for_update\|advisory\|lock` worker+repos = 0). 2 msg cùng conv_id: cả hai load pre-state thiếu nhau rồi ghi song song. messages order by `created_at` (conv_repo:260; models.py:307 func.now()) — KHÔNG cột sequence monotonic → tie nondeterministic | SỰ THẬT | Bot action multi-turn (booking slot): T2 condense/slot trước T1 persist → mất ngữ cảnh / slot ghi đè. Không config-fix tầng bot | Async Rule#7/#8 (ordering-sensitive không lock), S5 fail-loud (race silent), tính đúng multi-turn | Redis lock `ragbot:convlock:{conv_id}` quanh load→generate→persist, hoặc stream partition by conv_id; thêm cột sequence/turn_index monotonic |
| **RQ-1** | Sparse-branch query preprocessing pin tiếng Việt — non-VN bot bị vi-only tokenize, không hook config | retrieval-QUALITY | **P1** | pgvector_store.py:404 `segment_vi_compounds` không có lang arg → DEFAULT_LANGUAGE='vi'; :410 `strip_vn_filler_tokens` không override → `_DEFAULT_VN_FILLER_TOKENS` (vi); :411 `remove_diacritics` vô điều kiện. `hybrid_search()` 0 param language. text_utils.py:8 docstring claim override qua system_config nhưng call-site :410 không reach được (dead config contract). Ingest CÓ gate (ingest_stages.py:899) → asymmetry | SỰ THẬT | Onboard bot non-VN cần CODE CHANGE thread language vào hybrid_search — không config-only. Vi phạm 0-code-new-bot | S2 N+1, Domain-neutral (vi literal trên path multilingual), docstring sai vs reachable behaviour | Thread bot language vào `hybrid_search` + `strip_vn_filler_tokens`; đọc `vn_filler_tokens` từ system_config tại call-site |
| **RQ-2** | article_aware `@>`-filter áp lên corpus 0 metadata article_no → 0-row hard filter, cứu bằng full 2nd hybrid round-trip mỗi structural query | retrieval-QUALITY | **P1** | DB: legal thong-tu-09-2020 = 0/73 chunk có `metadata_json ? 'article_no'`. system_config metadata_filter_provider='article_aware', enabled=true. article_aware_filter.py:141 extract→{'article_no':'3'}. retrieve.py:789 add UNCONDITIONAL. hybrid `@> '{"article_no":"3"}'` trên cả dense+sparse → 0 rows. retrieve.py:1387 relax fallback (ON) re-run meta_filter=None → 2× round-trip + mất precision | SỰ THẬT | Mọi structural-doc corpus ingest THIẾU article_no metadata (legal bot hiện tại, memo 'Điều 11 miss') ăn 2× retrieve latency + degraded precision. Pre-filter net-negative | S4 SOTA (verify label-coverage trước hard-AND), T2 cost/perf (2nd round-trip), S5 fail-loud (0-row degrade silent, không metric coverage-mismatch) | Verify label-coverage trước khi add filter; nếu corpus 0 article_no → skip filter; thêm metric label-coverage mismatch |
| **OBS-1** | Empty LLM answer (completion-nhưng-no-text OR streamed-empty) đánh status=success không warning — silent masking | observability | **P1** | generate.py:766-814,967-985 trả answer_type='answered' với answer='' KHÔNG guard/log (grep `if not answer`/`len(answer)`/`empty_generation`=0). pipeline.py:622-629 `answer_text=...or ''` không check; callbacks.py:105 store `answer_text or '(empty)'`; callbacks.py:230-241 status='success'. Chỉ refuse_short_circuit (generate.py:287) emit empty intentionally | SỰ THẬT | Bot mới thừa hưởng path silent (step_tracker wired canonically graph_assembly.py:190). Operator không phân biệt empty-turn vs real không join `model_invocations.completion_tokens=0` tay | S5 fail-loud (empty-output phải observable), Coverage-rate mandate (empty khi corpus có data = silent failure phải đo được) | generate.py sau LLM-resolve: nếu `answer.strip()==''` and not refuse → `logger.warning('generation_empty_answer', ...)` + flag step('generate'). Domain-neutral |
| **OBS-2** | completion_tokens=0 trên 103/755 (13.6%) successful generation CÓ answer text — token/cost telemetry undercount trên streaming path | observability | **P1** | DB ragbot_v2_dev: `model_invocations purpose='generation'` → 755 success/'stop', zero_comp=103; cross-join request_logs: cả 103 có answer_hash NOT NULL + cached=False. Root: query_graph.py:1128-1137 `_capture_usage`; nếu final chunk thiếu usage → `_stream_usage['completion_tokens']`=init 0 (:1122) ghi as-is (:1182,:1192); invocation_logger.py:224,237 persist 0 | SỰ THẬT | Mọi bot streaming path (innocom qwen3) → cost rollup undercount ~14% turn. Không cần per-bot fix khi harden capture — platform-wide | S5 fail-loud (token=0 trên answered turn phải log), T2 cost-observability | Khi stream finish answer!='' nhưng completion_tokens==0 → `logger.warning('stream_usage_missing')` + fallback estimator (len(buffer)). Verify qwen3 adapter forward usage chunk |
| **DLC-3** | Retrieval bỏ qua `documents.state` — doc 'failed'/degraded vẫn serve chunks; comment 'readiness gate serves only state=active' SAI | doc-lifecycle | P2 | pg_bm25_retrieval.py:116-118 JOIN chỉ `d.deleted_at IS NULL` (no state); pgvector_store.py:270 chỉ `doc_deleted_at IS NULL`. grep `state='active'` retrieval+vector+retrieve.py = 0 gating. ingest_stages_final.py:198 comment claim gate state=active (sai) | SỰ THẬT | Re-index doc rồi fail embed → old+partial-new chunks vẫn retrievable. 'failed' label cosmetic for serving. Affects mọi bot | S4 correctness — comment document gate không tồn tại; latent trap | Quyết intent: degraded-serve → fix comment; gate → add `AND d.state=ANY(:servable_states)` cả 2 store (BM25-degraded config-gated) |
| **DLC-4** | progress_percent 1 checkpoint (20%) rồi nhảy 100 — embed phase (dài nhất) không show progress; ETA sai | doc-lifecycle | P2 | `_update_doc_progress` gọi đúng 1 lần: ingest_stages.py:815 progress=20 sau U4. U5/U6/U7(embed=N call) không emit. finalize ingest_stages_final.py:328 nhảy 100. UI document_routes.py:143 ETA=_age_s*(100-pct)/pct với 20 đứng yên → ETA sai; embed 30-min show '20%' suốt | SỰ THẬT | Mọi bot ingest doc lớn show frozen 20%→100% — structural, không per-bot fix | S4 observability — `_embed_in_doc_batches` đã iterate per-batch nhưng chỉ log | Emit `_update_doc_progress` per embed batch (30→90% scale by index) + current_step='embedding' trước U7 |
| **DLC-5** | DocumentState domain enum (DRAFT/PUBLISHED/...) phân kỳ hoàn toàn với runtime lowercase (active/failed/processing); transition guard dead | doc-lifecycle | P2 | shared/types.py:64-72 Literal TitleCase; document.py:29-31 _TRANSITIONS DRAFT→{PUBLISHED,...}. Nhưng pipeline ghi raw-SQL 'active' (ingest_core.py:505), 'failed' (ingest_stages_store.py:455), model default 'active' (models.py:336). Worker UPSERT/finalize overwrite lowercase KHÔNG qua `_transition()`. State machine không chạy trong ingest | SỰ THẬT | Platform-wide: 2 vocabulary không tương thích. Code tin DocumentState Literal (future state_filter) silently never match 'active'/'failed' | S3 open-closed / S1 model integrity — guard 0 protection | Chọn lowercase canonical (retrieval/recovery key vào đó), extend Literal thêm 'active'/'failed'/'processing', retire hoặc align _TRANSITIONS |
| **DLC-6** | tool_name derive 2 cách (slugify vs lower+replace) giữa use-case create vs worker UPSERT — latent uq_doc_tool dup-row | doc-lifecycle | P2 | ingest_document.py:63 `slugify(name)[:255]` (hyphen, 255); ingest_core.py:525 `title.lower().replace(' ','_')[:64]` (underscore, 64). uq_doc_tool(tenant,bot,tool_name) (models.py:315). Standard flow có existing_doc_id→UPDATE không rewrite (latent); INSERT branch is_reindex=False → tool_name mà get_by_tool_name(slugify) không tìm thấy → dup logical doc | SỰ THẬT | Latent: trigger cho caller gọi ingest() không existing_doc_id (scripts, future direct-ingest). Affects dedup correctness | S3 open-closed / single-source-of-truth | Extract 1 helper `slug_tool_name(name)` dùng CẢ 2 nơi |
| **MT-5** | Standalone `condense_question` node DEAD dưới config mặc định (merge_condense_router=True) | conv/multi-turn | P2 | routing.py:56-62 merge=True (default ca 2 path) trả 'understand_query'; chỉ False trả 'condense_question'. query_graph.py:2701 register node + :2770 edge nhưng 0 path reach default. Logic duplicate understand.py:157-176. condense_question.py:99 có re-apply numeral-normalize mà understand.py:165-176 KHÔNG → drift nếu merge=False | SỰ THẬT | Không ảnh hưởng bot mới trực tiếp; maintenance-trap: 2 bản condense phân kỳ (understand.py thiếu `normalize_vn_section_numerals`) | Simplicity-First / dead-node (anti-pattern 'orphan stub'), DRY | Gỡ condense_question node+edge (nếu merge canonical) HOẶC đồng bộ logic; live path đang thiếu numeral-normalize |
| **RQ-3** | ZE zembed-1@1280 anisotropic — dense branch yếu discriminative; precision dựa gần như toàn bộ vào BM25+rerank | retrieval-QUALITY | P2 | DB-measured cosine random-pair: chinh-sach-xe=0.914 (sd 0.145), test-spa=0.728, legal=0.757. Anchor-vs-others avg: xe 0.598, spa 0.668, legal 0.752. Xe: irrelevant chunk ~0.91 → relevant-vs-irrelevant gap single-digit% → dense ORDER BY cosine noisy. Chunks NOT heading-only (legal min 380 char) → compression intrinsic matryoshka-1280 truncation | SỰ THẬT | Cross-bot: bot dựa dense alone (BM25 Null hoặc bm25_weight thấp) → low recall@k. RRF vector=0.5/bm25=0.5 load-bearing — platform implicitly BM25-dependent. Semantic-only bot mới underperform | S4 SOTA (matryoshka-1280 truncation cost chưa quantify vs native 2560; no whitening), T1 (dense recall dựa lexical crutch) | Quantify 1280 vs 2560; cân nhắc whitening/calibration; giữ BM25 hybrid load-bearing |
| **RQ-4** | rerank_cliff_absolute_floor live=0.20 = 4× code-default 0.05, chưa calibrate scale zerank-2 — risk cắt chunk đúng low-confidence không recovery | retrieval-QUALITY | P2 | system_config floor=0.2, gap_ratio=0.5. Code-default 0.05/0.35 (constants/_01:162). zerank-2 raw relevance_score no normalize (zeroentropy_reranker.py:385-394). retrieval_filter.py:94 drop score<0.20 TRƯỚC gap-cut, giữ top min_keep (3, legal=5). Safety-net (rerank.py:457 N=2) re-inject RETRIEVAL-ordered KHÔNG floored-reranked — chunk rerank 0.15 dropped no recovery. gate_after_cliff=False (đúng, tránh double-gate) | GIẢ THUYẾT | Hard cross-lingual/paraphrase/multi-hop mà zerank-2 score <0.20 silently cut mọi bot dùng cliff. Không quantify được không live ZE rerank load-test (READ-ONLY) | S4 SOTA / Threshold-drift-post-migration (floor chưa recalibrate sau Jina→ZE; constants 0.05 vs DB 0.2 drift), measure-before-claim | Chạy `diagnose_p95_bottleneck.py --rerank-score-histogram` set floor empirically; safety-net re-inject từ floored-reranked không phải retrieval-ordered |
| **RQ-5** | chunk_quality scorer dead code — quality signal tồn tại nhưng không wire vào retrieval/ingest | retrieval-QUALITY | P2 | heuristic_chunk_quality_scorer.py:1-37 DEAD-CODE NOTICE 2026-06-03 (impl commented). shared/chunk_quality.py + port + null tồn tại nhưng 0 DI binding (AST import-graph + 10-agent audit). 0 chunk-level quality gate ingest, 0 retrieval-time re-weight | SỰ THẬT | Boilerplate/low-info chunk pollute index, compete RRF; no per-chunk quality prior. Cross-bot recall/precision tax. Port+Registry+Null built (S3 OK) nhưng inert | S3 open-closed (scaffold present unwired), T1 (no quality gating), orphan-node anti-pattern | Wire chunk_quality vào ingest gate + retrieval re-weight qua DI binding |
| **OBS-4** | Sub-step failure (multi_query_fanout embed CB-open) degrade single-query nhưng turn report success — observable trong request_steps nhưng no INFO 'degraded' event turn-level | observability | P2 | DB: request_steps multi_query_fanout status='failed' n=32; join request_logs → 26 'failed' + 6 'success'. Error="embedding CB open: 'embedder:jina' OPEN" (n=31)+1 HTTP-403. retrieve.py:1259-1264 degrade [query_text] + logger.warning('multi_query_node_failed'). 6 success turn silently collapse single-query (reduced recall), observable chỉ qua đọc request_steps.error tay | SỰ THẬT | Bot dùng (dead) jina embedder binding hit này; CB-open từ OLD jina còn binding sau ZE swap. Degrade silent turn-level (no metric alert '% turn single-query') | S5 fail-loud (degrade phải counted/alertable không buried per-step error); surface stale jina binding post-ZE | Counter `retrieval_degraded_total{reason='embed_cb_open'}` + audit event khi collapse single-query; audit binding còn point 'embedder:jina' sau swap |
| **OBS-3** | Stale observability claim memory (12/27 instrumented) — thực tế 32 distinct step_name live DB | observability | P2 | grep `.step("…")` orchestration = 33 distinct; DB request_steps = 32 distinct emit (+ ingest_* + hash_lookup_cache). Memory vẫn '12/27, 15 NOT_INSTRUMENTED'. Top-5 cũ (prompt_build, citations_extract, multi_query_fanout, rrf_fuse, litm_order) ĐỀU đã wired (verified DB) | SỰ THẬT | Documentation drift; no runtime impact nhưng audit mis-target 'blind step' thực ra instrumented | DAILY_REPORT truth-of-record accuracy | Update project_pipeline_24step_status / project_pipeline_observability: coverage 32(query)+7(ingest_*) live |

---

## 2. SỨC KHỎE 4 LUỒNG

### 2.1 retrieval-embedding-QUALITY (T1 — TRỌNG TÂM) — **AT-RISK**
**Basis**: Core ZE@1280 swap **mechanically CORRECT, verified DB (SỰ THẬT)**: vector(1280) khớp embedder dim 1280; 222/222 chunk embedded, 0 NULL (không retrieval-hole); asymmetric input_type ĐÚNG cả 2 phía (query task='retrieval.query'→input_type='query' zeroentropy_embedder.py:181; ingest 'retrieval.passage'→'document'); HNSW cosine m=32 live. RRF fusion sound (FULL OUTER JOIN rank-based, weights clamped≥0); cliff vs threshold-gate double-gating tránh đúng; metadata-zero relax fallback tồn tại. **"It runs" = TRUE và KHÔNG chạy blind.**
**NHƯNG quality at-risk 5 mặt**: (RQ-3 SỰ THẬT-measured) matryoshka-1280 anisotropic (xe random-pair 0.91) → dense branch yếu, precision dựa BM25+rerank; (RQ-1 SỰ THẬT) sparse query preprocessing vi-pin → non-VN bot block không code; (RQ-2 SỰ THẬT) article_no filter pure-waste + 2× round-trip trên legal corpus 0 label; (RQ-4 GIẢ THUYẾT) cliff floor 0.2 = 4× uncalibrated jump từ default 0.05, no recovery cho chunk cut; (RQ-5 SỰ THẬT) chunk_quality dead.
**Không vi phạm HALLU=0 sacred.** Verdict "GOOD or just running": **running correctly, nhưng coherence/precision UNVERIFIED-by-load-test + 1 weakness đo được (anisotropy) + 2 config/coverage mismatch.**

### 2.2 conversation / multi-turn — **AT-RISK**
**Basis**: Condense CÓ chạy (merged vào understand_query, default ON); semantic-cache multi-turn poisoning ĐÃ chặn (skip_multi_turn default True cả check_cache.py:61-65 + persist.py:154-162 — verified SỰ THẬT). NHƯNG: **P0 MT-1** (2 kho history disjoint theo entrypoint → mất memory cross-transport), P1 MT-2 (heuristic bypass condense → follow-up trật retrieval), P1 MT-3 (rolling-summary bất đối xứng + hardcode VN inject), P1 MT-4 (không serialize per-conversation → race ordering). Tất cả có evidence file:line. **Chưa load-test multi-turn định lượng → BASELINE TĨNH.**

### 2.3 document-lifecycle — **AT-RISK**
**Basis**: Happy-path solid: chunk delete-stale+delete-changed+insert+state-flip trong 1 session/commit (ingest_stages_store.py:650-972 atomic; embed-fail raise TRƯỚC mọi DELETE → no orphan); finalize atomic (ingest_stages_final.py:246-360); semantic_cache corpus_version-gated → stale answer auto-bypass; delete purge chunks+cache+corpus_version. NHƯNG **2 P1 wiring gap**: DLC-1 (header-idempotency không bao giờ 'done' → BE-to-BE retry contract vỡ), DLC-2 (state='failed' kẹt — transient 429 = dark vĩnh viễn) + state-machine divergence (DLC-5) + progress non-monotonic (DLC-4). **EVOLVE không REWRITE: mọi fix = wire-completion + reconciliation, framework đúng.**

### 2.4 observability-errors — **AT-RISK**
**Basis**: Spine healthy + auto-wire bot mới (step_tracker canonical graph_assembly.py:190; structlog inject trace_id+tenant+bot ctxvars; atomic single-INSERT model_invocations tránh stuck 'running'; 32 step_name live; finish_reason persisted; CB errors visible request_steps.error; quota/timeout/pipeline-fail fail-loud + error_notify_hook). Memory '15 NOT_INSTRUMENTED' STALE — coverage tốt hơn nhiều. NHƯNG **2 masking gap success-path**: OBS-1 (empty LLM answer status=success no warning), OBS-2 (103/755=13.6% answered turn completion_tokens=0 streaming → cost undercount). Cả 2 không raise → evade fail-loud. (extra=-swallow structlog bug từ memory KHÔNG present trong orchestration.)

**Không luồng nào BROKEN.** Cả 4 = **AT-RISK** (chạy được nhưng có wiring-gap / silent-masking / quality-mismatch chưa verified runtime).

---

## 3. CHƯA VỚI TỚI (not_checked)

**conversation/multi-turn**:
- Chưa chạy load-test multi-turn thật (2-3 turn đại từ tham chiếu) đo coverage-drop MT-2 — mới code-evidence.
- Chưa đo token-budget khi history dài (rolling chỉ apply 1/3 path; truncate MAX_HISTORY_LIMIT_REQUEST=20/DEFAULT_MAX_HISTORY=10).
- Chưa verify DB 3 bot (spa/xe/legal) có action_config.enabled không → không biết bot nào dùng conversation_id-path vs chat_histories-path.
- Chưa trace conversation_state_port (slot JSONB) load/save trên turn thật.
- Chưa kiểm request_steps instrumentation history_load/condense đầy đủ; chưa kiểm turn_count khi 2 entrypoint ghi 2 store.

**document-lifecycle**:
- DB-level state distribution (psql 10.0.1.160 unreachable) — không confirm bao nhiêu doc kẹt 'processing'/'failed'; DLC-1/DLC-2 impact magnitude = code-evidence-only.
- mark_done/mark_failed có wired ở branch/worktree khác chưa.
- Concurrency stress progress_percent under parallel re-ingest same doc_id (no monotonic guard).
- Late_chunking/sliding embed length-mismatch frequency với ZE zembed-1@1280.
- Brand-new bot upload high-volume tabular sheet end-to-end (narrate config-OFF).

**retrieval-QUALITY**:
- Live ZE embed/rerank API score distribution — BARRED READ-ONLY. RQ-3 anisotropy từ stored vectors only; RQ-4 cliff floor unquantified — cần `--rerank-score-histogram` + load-test.
- Answer-rate/Coverage/Faithfulness delta ZE@1280 vs Jina/2560 — no A/B.
- semantic_cache post-TRUNCATE (1024→1280); MMR cosine math; multi-query/decompose/HyDE recall-lift; BM25 ts_rank_cd normalization_flags=5 appropriateness.

**observability**:
- SSE consumer-side observability (chat_stream.py) — chỉ đọc producer-side.
- Prometheus metric cardinality cho counter mới.
- 12 status='failed'/finish_reason=NULL generation correlate error type nào.
- grounding_check sub-call degrade behavior trên exception.
- **Live verify innocom qwen3 stream adapter forward usage chunk** — root-cause OBS-2 ở adapter level là GIẢ THUYẾT; completion_tokens=0 DB fact là SỰ THẬT.

---
*READ-ONLY sweep. KHÔNG sửa code. Severity = code+DB evidence (BASELINE TĨNH), runtime impact CHƯA verified bằng load-test.*
