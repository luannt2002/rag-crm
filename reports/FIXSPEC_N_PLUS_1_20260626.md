# FIX-SPEC N+1 — Tổng hợp 4 flow-debug (ingest · provider/LLM · answer/guard · arch)

> Evidence verified 2026-06-26, branch `fix-260623-ingest-expert`, DB `ragbot_v2_dev` (10.0.1.160).
> 3 bot mẫu: `chinh-sach-xe` (retail/xe), `test-spa-id` (spa), `thong-tu-09-2020-tt-nhnn` (legal).
> Mindset: CLAUDE.md — EVOLVE không REWRITE; app KHÔNG inject/override answer; HALLU=0 sacred; zero-hardcode; domain-neutral.
> **N+1 test**: "bot ngành thứ N+1 chạy đúng với 0 dòng sửa `src/` — chỉ config DB (custom_vocabulary / bot_model_bindings / system_config / plan_limits / language_packs / alembic-seed)?"

---

## 1. KHUNG TIÊU CHÍ — "dựa vào đâu nhận xét + đề xuất"

| Tiêu chí | Định nghĩa 1 dòng | Cách áp khi chấm 1 issue |
|---|---|---|
| **S1 — Sacred / domain-neutral / no-app-override** | Code KHÔNG hardcode brand/giá/ngành; app KHÔNG inject text vào prompt, KHÔNG override answer LLM; HALLU=0; zero-hardcode magic number; resolve qua DI/Port không substring tên. | Hỏi: "có literal ngành/giá/tên-model nhúng trong logic không? app có thay/chèn answer không?" → có = vi phạm S1. |
| **S2 — N+1 (0-sửa-src cho bot mới)** | Bot ngành mới chạy đúng CHỈ bằng config DB, không sửa `src/`. | Hỏi: "ngành phi-thương-mại (legal/pharma/HR) ingest+serve đúng không sửa code? owner có knob khai không?" → không = vỡ N+1. |
| **S3 — Open-Closed / DI-Port** | Thêm capability/provider/route = thêm 1 adapter+config, KHÔNG sửa orchestrator; orchestration chỉ import Port. | Hỏi: "thêm route/role/provider mới có phải sửa thân hàm core không? orchestration có import infra concrete không?" → có = vi phạm S3. |
| **S4 — RAG SOTA (attribute-generic / table-RAG)** | Cấu trúc bảng giữ MỌI cột là typed-attribute (TableRAG/MixRAG late-binding); cross-table entity resolution; không collapse non-price→noise. | Hỏi: "non-price column có typed-home queryable không? entity spread đa-sheet có reconcile không?" → không = vi phạm S4. |
| **S5 — Fail-loud vs silent-degrade** | Lệch giả định (judge dead, header mangle, provider 429) phải fail-loud/observable, KHÔNG degrade thầm trả kết quả sai/chưa-verify. | Hỏi: "khi component này chết/mis-parse, owner/ops có thấy không, hay answer vẫn đi thầm?" → thầm = vi phạm S5. |

---

## 2. FIX-SPEC TỪNG VẤN ĐỀ

### 2.A — INGEST stats-index flow

| Issue | Hiện trạng (file:line) | Vi phạm | Vỡ N+1 thế nào | FIX config-driven | Tier | Effort |
|---|---|---|---|---|---|---|
| **ING-2** PRICE_MIN_VND floor conflate "not-a-price" với "discard"; không có quantity-role → stock/count → col_N noise | `document_stats.py:222-245` `parse_money_vn()` floor `DEFAULT_PRICE_MIN_VND=10_000`; `number_format.py:147` `_guard` return None <10k; chỉ 4 role token `_NAME/_CATEGORY/_PRICE/_ALIASES`. DB: `chinh-sach-xe` stock '26'/'404' → `attributes_json.col_3` | S4 (numeric phải first-class), S1 (floor 10k = giả định VN-commercial), S5 (mis-key col_N silent) | Pharma dosage 500mg, insurance term 12mo, inventory stock 404 — sub-10k integer có ý nghĩa rơi col_N untyped, "how many in stock" không có index-path; owner không khai được quantity-role | Thêm `_QUANTITY_COL_TOKENS` frozenset + alias 'quantity/value/number' vào role enum; `parse_money_vn` trả integer thô, **floor chỉ áp ở caller cho cell bound price-role**; quantity-cell giữ mọi positive int → persist `attributes_json['__quantity__']` range-queryable. Config: floor giữ `system_config`, consult CHỈ khi price-role. N+1-proof: numeric column mọi magnitude có typed-home qua cùng Tier-1 inference + Tier-2 owner `column_roles` như price | T1 | M |
| **ING-3** ParsedEntity PRICE-CENTRIC — chỉ name/price/alias first-class; quantity/date/media/code → col_N, mất khi header glitch | `document_stats.py:248-272` dataclass slot chỉ name/category/price_primary/secondary/aliases; `_extract_entity_from_row:485-684` everything-else → `attributes[col_label]` (590-591). DB: legalbot 108 entity / **0/108 price** = prose false-positive | S4 (TableRAG late-binding keep ALL typed), S1 (price-first = commercial worldview vào core extractor), **ADR-0007 propose chưa code** | Insurance/real-estate/pharma defining-column (coverage/area/dosage/expiry) non-price → flatten col_N; structural index chỉ trả lời name+price. Ngành mới = stats index vô dụng, không fix bằng config | Theo ADR-0007: thay dataclass price-pinned bằng attribute-generic — name+category+aliases + ALL value-column vào typed `attributes` keyed (role, header_label), role ∈ {price,quantity,date,media,code,text} resolve qua `_column_roles` (Tier-2 owner AUTHORITATIVE > Tier-1 token-set). `price_primary/secondary` giữ làm **derived VIEW** over role=price (backward-compat SQL). N+1-proof: thêm ngành = thêm token vào role frozenset HOẶC owner khai `column_roles`; ParsedEntity shape không đổi | T1 | L |
| **ING-4** Two-row/merged header mangle → real column → col_N; entity_name lấy warehouse-stub | `_premerge_split_headers:766-795` merge CHỈ khi `_is_header_row` exact-match `_HEADER_EXACT_TOKENS`. DB: 251/496 xe row mang col_N; entity_name='Kho lốp các loại' (stub), product thật ở col_2, image col_4 | S4 (header reconstruct phải robust không exact-token gate), S1 (built-in vocab gate detect), S5 (mis-parse silent) | Header ngoài built-in token (non-VN-commercial) → header un-recognised → premerge skip → mọi column shift col_N → entity_name = col_0 stub; owner không có knob ép "row N = header" | (a) Header DETECTION không phụ thuộc vocab — structural heuristic (row không có parseable price/number AND ≥2 short label-cell AND next-row data) + owner override `custom_vocabulary['header_row_index']`/`['column_labels']` (extend `_is_header_row` honor declared ordinal). (b) **Fail-loud structured event** khi >X% row → col_N. N+1-proof: header detect structural hoặc owner-declared, 0 src edit | T1 | M |
| **ING-6** Media/image URL treated as NOISE → chỉ sống col_N; 0/496 role-bound image | `document_stats.py:55-58` `_STATS_URL_NOISE_RE` DROP cell có http(s)/domain khỏi name (line 640); KHÔNG có media-role; name cap `DEFAULT_STATS_ATTR_MAX_CHARS=120` cắt URL dài. DB: 2/496 drive-link sống, cả 2 dưới col_4/col_5, 0 image-key | S4 (media = first-class catalog column), S1 (hardcode URL=noise), S5 (image drop → answer fabricate URL) | Catalog bot (e-commerce/real-estate/insurance-PDF) có media-column core → structurally un-indexable + scrub noise; "cho ảnh sản phẩm X" không có grounded URL → fabricate; owner không thêm media-role được | Thêm MEDIA role `_MEDIA_COL_TOKENS` ('hinh anh/image/anh/link/url/photo/media') + alias. Khi column media-role: (i) bypass URL-noise drop, (ii) bypass 120-char cap, (iii) persist `attributes['__media__']` query-layer ground real link. URL-noise regex CHỈ áp cell KHÔNG bound media-role. N+1-proof: inference bind 'Hình ảnh/Image/Link' header HOẶC owner khai `column_roles {'Ảnh':'media'}` | T1 | M |
| **ING-7** Stale-entity leak: `DeleteDocumentUseCase` không purge `document_service_index`; serving query không filter archived | `use_cases/delete_document.py:44-89` delete vector + archive doc NHƯNG không gọi `stats_index_repo.delete_by_document` (có sẵn `:152`, chỉ gọi từ re-index `ingest_stages_final.py:548`). Serving (`query_by_price_range:244`, `top_by_price:317`, `list_all_entities:423`, `query_by_name_keyword:522`) filter `record_bot_id` ONLY, no `deleted_at`. Table không có cột `deleted_at` | S5 (deleted corpus vẫn serve = correctness+compliance hazard), S1 (owner owns corpus) | **Universal mọi bot** không phụ thuộc data-shape: mỗi delete/re-upload-rename để orphan; superlative/list/count trả số từ doc đã xóa = HALLU-adjacent | **Pure wiring (EVOLVE, 0 config mới)**: (a) `DeleteDocumentUseCase` nhận `stats_index_repo` DI (Optional pattern như `DocumentService:194`), gọi `await stats_index_repo.delete_by_document(doc.id)` trong uow trước commit (keyed `record_document_id` global-unique). (b) Defence-in-depth: serving query JOIN `documents d ON d.id=i.record_document_id AND d.deleted_at IS NULL AND d.status='active'`. N+1-proof: domain-agnostic, no per-bot config | T1 | **S** ⭐quick-win |
| **ING-8** Cross-sheet entity không reconcile: dedup per-document → 1 product split price-sheet + stock-sheet = 2 entity rời | `_dedup_stats_entities:139-186` group (name, price_primary), gọi 1 lần/document `_stage_finalize:525`; `bulk_insert` keyed `record_document_id`; KHÔNG có cross-doc merge | S4 (cross-table entity resolution), S5 ("price AND stock của X" partial answer silent) | Multi-sheet workbook là norm (Giá-sheet + Tồn-kho-sheet; plans + premiums). Bot mới multi-sheet = fragmented entity; owner không có reconcile-knob | Bot-scoped reconcile keyed configurable entity-key: `custom_vocabulary['entity_key_role']` (default=name; owner set SKU/code). Reconcile pass idempotent MERGE `attributes_json` + role-slot across row cùng entity-key trong `record_bot_id` (hoặc query-time GROUP+COALESCE — cheaper, tránh write-contention). N+1-proof: join-key owner-declarable default name; depends ING-3 attribute-generic | T1 | L |

### 2.B — PROVIDER/LLM structured-output flow

| Issue | Hiện trạng (file:line) | Vi phạm | Vỡ N+1 thế nào | FIX config-driven | Tier | Effort |
|---|---|---|---|---|---|---|
| **PLM-5** Structured-output route theo TÊN wire (substring 'openai') không theo CAPABILITY → ép qwen3 strict json_schema | `structured_output_helper.py:389` `call_with_schema` → `_is_openai_compatible(litellm_name, provider_code)` (`:116-121`) build haystack `f"{name}|{code}"` (`:45-46`) substring-match `OPENAI_STRUCTURED_OUTPUT_PROVIDER_CODES=('openai','azure','azure_ai')` (`_14:170`). DB: chat 3 bot = `openai/claude`@`innocom`. `supports_json_mode=true` CÓ trong DB nhưng helper KHÔNG đọc. 33 usage | S1 (route by string không capability DB), S5 (ép strict → None/empty silent) | Bot mới engine relay wire-name chứa 'openai'/'azure' (`openai/llama3`, `azure-proxy/mistral`) bị ép strict dù engine không hỗ trợ → structured node (decompose/grade/understand/slot) trả None → answer rỗng. Ngược lại `innocom/gpt` rớt plain-fallback dù `supports_json_mode=true` | Route theo CAPABILITY: (a) thêm param `supports_json_mode: bool` (+optional `supports_strict_schema`) vào `call_with_schema`; (b) call-site `_invoke_structured_llm_node` (`query_graph.py:1324`) truyền `cfg.capabilities.supports_json_mode`; (c) dispatch 3-bậc: `supports_strict_schema`→json_schema strict; elif `supports_json_mode`→`response_format={'type':'json_object'}`+prompt-embed schema+`_fallback_json_parse` (precedent router:1030 qwen3 honor); elif `supports_tools`→tool_choice; else plain. `_is_anthropic` đọc cột `supports_tools` thay substring. Alembic thêm cột `supports_strict_schema` seed openai/azure=true. N+1-proof: bot mới set cờ capability trên `ai_models` row = 0 sửa code | T1 | M |
| **PLM-1A** `slot_extractor` resolve qua system_config alias 'haiku'→hardcode wire `openai/gpt-4.1-mini` (provider dead), bypass `bot_model_bindings` | `slot_extractor.py:168-183` `_resolve_model` đọc `system_config['slot_extractor_model']` default 'haiku' → `DEFAULT_SLOT_EXTRACTOR_MODEL_WIRE='openai/gpt-4.1-mini'` (`_20:62-64`), provider `openai` dead (429); KHÔNG dùng `model_resolver` | S1 (zero-hardcode wire-name + bypass Port), S2 (slot không lấy per-bot binding), S5 (provider dead → return {} silent slot rỗng) | Bot mới bật action/slot-filling resolve về provider dead → slot trả {} → action flow gãy thầm; không trỏ slot model sang engine sống trừ sửa constants global (vỡ per-bot isolation) | (a) Định nghĩa purpose 'slot' trong `BindingPurpose`; (b) `_resolve_model` gọi `model_resolver.resolve_runtime(tenant, bot, purpose='slot')` — fallback purpose→llm_primary đã có (`service.py:354-359`) nên bot không khai 'slot' tự dùng chat sống (innocom). (c) Re-point binding dead qua **alembic** (sacred#7 KHÔNG psql). (d) Bỏ alias-map + 3 constant wire openai. N+1-proof: bot mới chọn slot model qua seed `bot_model_bindings` row = 0 sửa code | T1 | M |
| **PLM-1B** Binding `purpose='enrichment'` 3 bot → `gpt-4.1-nano`@openai dead → ingest narrate gãy thầm | DB: 3 bot binding active enrichment → `gpt-4.1-nano`@`openai` (429). `chunk_context_enricher.py:218` graceful-degrade → chunk KHÔNG enrich, retrieval kém thầm | S5 (enrichment fail silent), sacred#7 nếu fix psql. **Config-drift KHÔNG code-bug** | Bot mới clone template kế thừa binding enrichment→dead → enrichment tắt thầm, retrieval kém không ai biết | (a) **Alembic** re-point binding enrichment 3 bot openai→provider sống. (b) Sửa seed/bot-creation template không default enrichment→openai dead (trỏ shared-default sống HOẶC bỏ binding để fallback llm_primary). KHÔNG sửa resolver (đã đúng). N+1-proof: enrichment do binding/template config | T2 | S |
| **PLM-1C** Binding inactive rank=1 grounding/grading → openai dead (thong-tu) — dead config rác | DB: thong-tu 2 binding **inactive** (active=f) grounding→`gpt-4.1-nano`(openai), grading→`openai/claude`(innocom). `list_bindings` default `active_only=True` (`ai_config_repository.py:388,410-411`) lọc → fallback llm_primary innocom (sống). Giả thuyết ban đầu "grounding dead 429" **KHÔNG đúng** | S3 noise only (inactive + fallback che). Foot-gun nếu ai set active=true | Không trực tiếp vỡ. Rủi ro: set active=true 2 row rác → grounding/grading trỏ dead → 429 | Optional: alembic xóa/re-point 2 binding inactive rác, HOẶC để nguyên (vô hại). 0 src | T3 | S |

### 2.C — ANSWER/GUARD flow (khung ĐÚNG sacred — chỉ 4 lỗ config + 1 compliance-note)

| Issue | Hiện trạng (file:line) | Vi phạm | Vỡ N+1 thế nào | FIX config-driven | Tier | Effort |
|---|---|---|---|---|---|---|
| **AG-A1** Anti-fabricate-URL THIẾU trong language_pack + bot sysprompt → bot bịa link | `generate.py:595-606` prompt = bot.system_prompt + assembler-append `language_packs.sysprompt_default_rules`. DB: `[vi].sysprompt_default_rules` rule#2 chỉ cấm bịa 'thông tin/giá/địa chỉ' — 0 hit URL/link. Chỉ `chinh-sach-xe` mention url; spa+legal không. grep http/url trong `orchestration/nodes/` = 0 hit logic | S1 (app KHÔNG override answer — phải fix bằng RULE config KHÔNG regex strip), S5 (bịa URL silent). **HALLU fabricate (sacred)** | Bot mới mọi ngành có link corpus → "cho link X" bịa URL plausible vì 0 rule cấm; grounding warn-only không chặn. Rule ở language_pack chung — thiếu thì MỌI bot locale đó thiếu | **0 sửa code answer-path**: thêm 1 rule-block domain-neutral vào `language_packs[code].sysprompt_default_rules` qua **alembic** (đúng ADR-W1-S10 seed/update chỉ alembic): "ANTI_FABRICATE_URL: chỉ đưa link nếu link XUẤT HIỆN NGUYÊN VĂN trong ngữ cảnh; không tự tạo/đoán/ghép URL". Assembler auto-append mọi bot cùng locale; per-bot opt-out `plan_limits.sysprompt_rules_disabled`. N+1-proof: bot mới auto-inherit | T1 | S |
| **AG-A2** Grounding judge degrade THẦM khi LLM grounding chết → HALLU-net tắt im | `guard_output.py:169-219` build `_grounding_llm` qua `resolve_runtime(purpose='grounding')`. DB: thong-tu grounding→`gpt-4.1-nano` dead. `local_guardrail.py:514-520` except → `grounding_degraded_total.inc` + log + **return None** → answer 'pass UNVERIFIED'; không nâng severity/refuse | S5 (judge dead phải fail-loud/refuse không degrade thầm), S1 (HALLU-net tắt runtime không ai biết) | Bot mới nếu binding grounding trỏ provider down HOẶC rate-limit tải cao → judge chết → MỌI answer pass unverified; owner không biết HALLU-guard đã tắt. Degrade-silent là default mọi bot | Config `grounding_failure_mode` đọc từ `pipeline_config` (`_pcfg`), default-safe. `llm_grounding_check` khi judge chết: mode='fail_open' (return None — bot chấp nhận rủi ro) HOẶC mode='fail_closed' (return hit severity='block' → guard_output substitute oos_template như nhánh GuardrailBlocked **đã có sẵn**). Root-cause: re-seed binding grounding thong-tu → model sống qua **alembic** (sacred#7). N+1-proof: bot mới chọn mode qua config, default fail_closed bảo vệ HALLU=0 | T1 | M |
| **AG-A3** Structured grounding judge bị ép strict json_schema do substring 'openai' trong 'openai/claude' (qwen3) → judge parse-fail thầm | `OutputGuardrail.llm_grounding_check` dùng `call_with_schema` → `_is_openai_compatible` (`:116-121`) substring-match (`_14:170`). DB: spa+thong-tu grounding name=`openai/claude`(wire qwen3) → ép strict → qwen3 không honor → parse fail → None → degrade (cùng AG-A2). `supports_json_mode` CÓ trong DB nhưng không đọc | S1 (route by name không capability), S5 (judge fail silent). **= PLM-5 ở serving** | Bot mới model tên chứa 'openai'/'azure' wire → provider không honor strict (qwen3/deepseek/self-host gateway 'openai/...') → MỌI structured call (grounding/grade/decompose/understand) fail thầm | **CHUNG fix PLM-5**: route theo capability `supports_json_mode` từ cfg (model_resolver đã resolve), phân biệt strict/tool/prompt-json bằng capability field KHÔNG substring tên. ~9 call-site qua `call_with_schema` hưởng chung. N+1-proof: model mới khai capability `ai_models` row | T1 | M |
| **AG-A4** `i18n.py` hardcoded VI refuse_message latent-fallback cho locale chưa seed (rule#3 risk) | `OosTemplateResolver` tier6 → `language_pack_service.py:116-137` chain Redis→DB(locale)→DB(vi)→`_inmemory_fallback`(`i18n.py:515-518` VI literal non-empty). DB hiện CÓ refuse_message vi+en (3 bot không chạm). NHƯNG locale mới ('es'/'th') chưa seed → fallback DB(vi)→i18n VI cứng | S1 rule#3 ("KHÔNG fallback i18n.py hardcoded — empty nếu bot không set"). tier7=""ĐÚNG nhưng tier6 inmemory chen TRƯỚC | Bot mới `language='es'/'th'/'ja'` chưa seed → OOS refusal trả VI cứng (sai ngôn ngữ + vi phạm rule#3); owner không tự set được refuse text đúng-ngôn-ngữ nếu chưa alembic seed | `language_pack_service` expose `get_strict(locale,key)` (chỉ DB, KHÔNG inmemory) cho refuse_message consumer — HOẶC `OosTemplateResolver` phân biệt DB-hit vs inmemory-fallback, bỏ qua inmemory cho refusal → rơi tier7="". i18n inmemory chỉ phục vụ boot-outage prompt-keys khác. N+1-proof: locale mới chưa seed → OOS empty (rule#3) thay VI cứng; owner seed alembic là đủ | T2 | S |
| **AG-A5** Refuse-short-circuit + grade-all-failed path ĐÚNG sacred — compliance note | `generate.py:255-300` refuse 0-graded → `_oos_text` (DB 7-tier) or `DEFAULT_OOS_ANSWER_TEMPLATE`(""), KHÔNG i18n/inject. `grade.py:402-437` all-fail → ambiguous không refuse oan. `guard_output.py:60-68` grounding observability-only KHÔNG substitute | — (ĐÚNG) | KHÔNG vỡ — baseline đúng N+1-safe | **KHÔNG fix.** Ghi nhận baseline-đúng để A1-A4 KHÔNG vô tình refactor đập (đừng thêm app-side URL-strip ở A1, đừng thêm app-side refusal literal ở A4) | T3 | N/A |

### 2.D — QUERY ORCHESTRATION (arch)

| Issue | Hiện trạng (file:line) | Vi phạm | Vỡ N+1 thế nào | FIX config-driven | Tier | Effort |
|---|---|---|---|---|---|---|
| **ARCH-6** `document_service_index` PRICE-CENTRIC schema + stats-route early-binding | DB cột: entity_name, entity_category, **price_primary/price_secondary NUMERIC first-class**, attributes_json, entity_synonyms. `retrieve.py:200-239` stats-route parse price/range/code query `WHERE record_bot_id + price-range`. 3/3 bot `column_roles=f` → 100% Tier-1 price inference. ADR-0007 propose chưa code | S4 (attribute-generic/late-binding), S1 (price=commercial bias vào SCHEMA), S2 (legal không có giá → stats-route vô dụng) | Bot non-giá (legal/medical/HR): attribute quan trọng (số điều, liều, ngày hiệu lực) không có cột first-class → mất exact-lookup → rơi vector (yếu với heading-only). Schema buộc mọi ngành map về price = FAIL | **Thực thi ADR-0007 PRICE→ATTRIBUTE** (= ING-3 ở serving): thay price cột bằng `attributes` JSONB generic {role:value} + `numeric_attributes` index-able range. `column_roles` khai `custom_vocabulary` (Tier-0 owner) map header→role. stats-route → attribute-range-route theo role bất kỳ. `price_primary/secondary` giữ derived-VIEW backward-compat. N+1-proof: ngành mới khai `column_roles` = 0 sửa code | T1 | L |
| **ARCH-5** 125 pipeline_config key consume rải rác không phân lớp; key trùng nghĩa | grep `_pcfg(state,...)` = 125 distinct. (A) per-bot ~60 hợp lệ, (B) thuần-kỹ-thuật ~25 nên constants, (C) dead ~10, (D) trùng nghĩa: `rrf_k`/`rag_rrf_k`/`lexical_rrf_k`; `rerank_top_n`/`_by_intent`; `mmr_*`/`_by_intent` | Simplicity-First (125 knob = bot owner choáng); KHÔNG zero-hardcode (đã config). **GIẢ THUYẾT** phân-lớp chưa load-test | Bot mới đối mặt 125 knob không "profile mặc định theo ngành" → bỏ trống (default price-centric fail) hoặc phải hiểu cả 125. Cặp trùng (3×rrf_k) drift: set 1 quên 2 | KHÔNG xóa key. (1) Phân 3 tier doc: TENANT-FACING ~60 / OPERATOR ~25 / DEAD (dọn cùng ARCH-2/3). (2) Gộp trùng: `rrf_k` SSoT, còn lại alias/xóa. (3) **CONFIG PRESET per-domain**: `system_config pipeline_preset.{retail\|legal\|medical}` = dict default → bot mới chọn 1 preset thay 125 lẻ. `_pcfg` thêm preset-layer (preset < system_config < plan_limits < column). N+1-proof: bot mới chọn preset = 1 key | T2 | M (research-heavy) |
| **ARCH-1** God-node `retrieve.py` — 1 hàm async 1700 dòng, 22 kwarg DI, 4 closure lồng | `retrieve.py:149-1849` 1 block: stats-route + VN-preprocess (`643-1070`) + embed + RRF + multi-query + metadata + fallback. Header tự ghi "byte-identical pure relocation" | S3 (Open-Closed: capability mới phải sửa thân); Karpathy#3. KHÔNG sacred trực tiếp nhưng rủi ro bug cao nhất (96-131KB) | Bot mới ngành khác cần route khác (mã-hồ-sơ thay giá) phải chèn if vào god-node = sửa src → FAIL N+1 | EVOLVE tách sub-stage thành module coroutine nhận state+DI (KHÔNG đổi state contract): `retrieve_stats_route` / `retrieve_preprocess` / `retrieve_hybrid` / `retrieve_fanout`; `retrieve()` chỉ orchestrate. Route-selection → `RetrievalRouteStrategy` Port+Registry (`retrieval_route_strategy` config). Cần golden-test pin chunk-output byte-identical TRƯỚC khi tách. N+1-proof: route mới = thêm 1 adapter | T3 | L |
| **ARCH-3** 2 decomposer entry-node trùng contract (decompose + adaptive_decompose) | `decompose.py:30` (intent multi_hop/comparison, `routing.py:111`) + `adaptive_decompose.py` (complexity=complex, `routing.py:95`) — CẢ HAI ghi `sub_queries`, CẢ HAI edge→retrieve, 2 prompt khác. `query_decomposer.py` file thứ 3 chỉ import bởi adaptive | Karpathy#2 Simplicity (2 đường 1 capability), S3 (sửa decompose phải nhớ 2 chỗ) | Bot mới: bật adaptive → query_decomposer prompt; tắt → prompt_decompose — 2 chất lượng split cho cùng câu hỏi, không deterministic; fix prompt 1 chỗ path kia drift | Hợp nhất 1 decompose-node, 2 trigger (complexity-class vs intent-label) GỌI CHUNG 1 impl + 1 prompt SSoT (`query_decomposer.decompose_query`); giữ 2 router-gate nhưng cùng route→'decompose'; xóa prompt trùng + adaptive wrapper. Cần test pin sub_queries cả 2 trigger. N+1-proof: 1 đường = deterministic per-bot | T3 | M |
| **ARCH-4** DI-leak — orchestration import infra concrete 6 site thật | grep `from ragbot.infrastructure` trong orchestration: `query_graph.py:35` OutputGuardrail, `:36` InvocationLogger, **`:304` dynamic_litellm_router.compute_cost_usd** (vi phạm trực tiếp pre-commit guard), `:1083` speculative_router; `graph_assembly.py:35` KnowledgeGraphService; `graph_retrieve.py:9`; `guard_output.py:28`; `retrieve.py:141,1499`; `persist.py:143` math_lockdown (đọc-only cache, KHÔNG override — verified) | S1/strategy-DI ("orchestration chỉ import Port"); pre-commit guard CLAUDE.md ghi rõ | KHÔNG vỡ runtime (cùng infra) NHƯNG khóa swap per-tenant + test phải kéo infra thật (brittle, không inject Null/Mock) | EVOLVE 3 leak nặng → Port: (1) compute_cost_usd → `CostCalculatorPort` inject; (2) OutputGuardrail → `GuardrailPort` (registry `build_guardrail` ĐÃ có `bootstrap.py:86`, chỉ chưa nối ở node); (3) KG → `KnowledgeGraphPort`. math_lockdown → pure util `shared/` (stateless). metrics giữ nguyên (side-effect-free). N+1-proof: swap = config provider string | T3 | M |
| **ARCH-2** `condense_question` DEAD NODE (merge_condense_router=true global) | `query_graph.py:2701` register + `:2770` edge→router; `routing.py:56-62` `_cache_route`: `merge_condense_router=True`→'understand_query'. DB `pipeline_merge_condense_router=true` + 0/3 bot override → LUÔN understand_query; `condense_question.py:26` (5KB LLM-call) không bao giờ chạy | Karpathy#2 Simplicity (dead code) + #3 | Không trực tiếp vỡ nhưng tăng surface: nếu bot mới set merge_condense_router=false → kích hoạt path chưa từng load-test → behavior bất ngờ (S5 risk) | CHỌN (a) GỠ node+edge+file+flag (xóa nhánh else trong `_cache_route`) — khuyến nghị vì merged path canonical; HOẶC (b) giữ opt-in + thêm unit test pin nhánh + docstring. Grep test-refs trước khi xóa. N+1-proof: ít node = ít config bot mới phải hiểu | T3 | S |

---

## 3. TỔNG — Dự án CHUẨN mindset CLAUDE.md chưa?

**KHUNG = ĐẠT (đáng GIỮ, EVOLVE không REWRITE).** Dựa vào evidence:
- Hexagonal/Port/DI/4-key chuẩn: `bootstrap.py` registry + Singleton/Factory + Port cho vector_store/embedder/reranker/lexical/guardrail/metadata_filter/entity_extractor; 4-key trên GraphState đầy đủ.
- **Sacred#2 (app KHÔNG override answer) TÔN TRỌNG**: `generate.py` đọc answer LLM verbatim; `guard_output.py:60-68` grounding warn-only KHÔNG substitute; `persist.py:143` math_lockdown chỉ DECIDE cache (đọc-only, comment verified). OOS template DB-driven 7-tier, tier7="" (đúng rule#3).
- Sysprompt qua `SysPromptAssembler` append `language_packs.sysprompt_default_rules` (đúng ADR-W1-S10 governed exception).

**Nhưng "DÂY CHƯA NỐI HẾT" → N+1 = NO (3/4 flow), PARTIAL (answer/guard).** Kết luận dựa vào: psql `column_roles=f` cả 3 bot · legalbot 108 entity/0 price · 251/496 xe row col_N · DB binding enrichment→openai dead · `supports_json_mode` cột tồn tại nhưng helper route by substring · grep 125 `_pcfg` key · grep 6 infra-import trong orchestration · `routing.py:60` + system_config merge_condense=true.

### Gap-list để bot-mới-hoàn-toàn chạy 0-sửa-code
1. **[BLOCKER] Attribute-generic stats index (ADR-0007)** — ING-3 + ARCH-6: ParsedEntity + schema price-centric → role enum {price,quantity,date,media,code,text}. Đây là gốc rễ N+1 fail.
2. **[BLOCKER] Route structured-output theo capability không substring tên** — PLM-5 + AG-A3 (cùng 1 fix, ~9 call-site hưởng chung): đọc `supports_json_mode`/`supports_strict_schema`/`supports_tools` từ cfg.
3. **Header detection vocabulary-independent** — ING-4: structural heuristic + owner `header_row_index`.
4. **Quantity + media role first-class** — ING-2, ING-6.
5. **slot_extractor resolve qua bot_model_bindings purpose-based** — PLM-1A (bỏ hardcode wire).
6. **Delete-purge stats index + archived-doc JOIN guard** — ING-7 (quick-win, domain-agnostic).
7. **Anti-fabricate-URL rule vào language_pack** — AG-A1.
8. **Grounding failure-mode config (fail_closed default)** — AG-A2.
9. **Cross-sheet entity reconcile** — ING-8 (depends ING-3).
10. **Config-preset per-domain** — ARCH-5 (giảm 125 knob → 1 preset key).
11. **Re-point binding dead qua alembic** (KHÔNG psql) — PLM-1B + AG-A2 root-cause.

---

## 4. THỨ TỰ XỬ LÝ

### P0 — 0-API / pure-wiring / 0-re-ingest (làm ngay, domain-agnostic)
| # | Việc | Issue | Tier | Effort | Cơ chế |
|---|---|---|---|---|---|
| P0-1 | Delete-purge stats + archived-doc JOIN guard | ING-7 | T1 | S | DI wiring + JOIN, 0 config |
| P0-2 | Route structured-output theo capability (PLM-5=AG-A3) | PLM-5, AG-A3 | T1 | M | param capability + alembic 1 cột; sửa grounding judge fail thầm |
| P0-3 | Grounding failure-mode config (fail_closed default) | AG-A2 | T1 | M | `_pcfg` mode + branch llm_grounding_check |
| P0-4 | slot_extractor resolve qua model_resolver purpose='slot' | PLM-1A | T1 | M | inject resolver, xóa 3 constant wire |
| P0-5 | DI-leak 3 Port (cost/guardrail/KG) + math_lockdown→shared util | ARCH-4 | T3 | M | nối Port (build_guardrail đã có) |
| P0-6 | Gỡ dead condense_question + flag | ARCH-2 | T3 | S | xóa node/edge/file |
| P0-7 | Hợp nhất 2 decomposer → 1 impl SSoT | ARCH-3 | T3 | M | gộp router-target |

### P1 — Re-ingest / alembic-seed (cần chạy lại corpus hoặc seed DB)
| # | Việc | Issue | Tier | Effort | Cơ chế |
|---|---|---|---|---|---|
| P1-1 | Anti-fabricate-URL rule → language_packs vi+en | AG-A1 | T1 | S | alembic seed rule-block |
| P1-2 | Header detection structural + owner header_row_index | ING-4 | T1 | M | re-ingest sau fix |
| P1-3 | Quantity-role + bỏ price-floor global | ING-2 | T1 | M | re-ingest |
| P1-4 | Media-role first-class | ING-6 | T1 | M | re-ingest |
| P1-5 | Re-point binding enrichment/grounding dead → sống (alembic) | PLM-1B, AG-A2 | T2 | S | alembic re-point + sửa seed template |
| P1-6 | i18n refuse_message get_strict (skip inmemory) | AG-A4 | T2 | S | OosTemplateResolver tier6 |
| P1-7 | Locale-fallback OOS empty thay VI cứng (rule#3) | AG-A4 | T2 | S | (cùng P1-6) |

### P2 — Architecture / schema-migration (lớn, cần golden-test 3 bot)
| # | Việc | Issue | Tier | Effort | Cơ chế |
|---|---|---|---|---|---|
| P2-1 | **Attribute-generic ParsedEntity + schema (ADR-0007)** — BLOCKER gốc N+1 | ING-3, ARCH-6 | T1 | L | alembic schema + ingest map role + serving generic-query + backfill |
| P2-2 | Cross-sheet entity reconcile (depends P2-1) | ING-8 | T1 | L | bot-scoped reconcile / query-time COALESCE |
| P2-3 | Config-preset per-domain (giảm 125 knob) | ARCH-5 | T2 | M | preset-layer trong `_pcfg`; cần load-test đếm key move-needle |
| P2-4 | Tách god-node retrieve.py thành strategy injectable | ARCH-1 | T3 | L | golden-test pin byte-identical TRƯỚC |
| P2-5 | Optional cleanup 2 binding inactive rác | PLM-1C | T3 | S | alembic optional |

**Nguyên tắc thứ tự**: P0 trước (0-rủi-ro, domain-agnostic, mở khóa correctness ngay — đặc biệt P0-2/P0-3 đóng HALLU-silent-degrade sacred); P1 re-ingest gom 1 batch (header+quantity+media+enrich) để chạy lại corpus 1 lần; P2 schema-migration cuối (BLOCKER N+1 thật là P2-1 ADR-0007 nhưng effort L cần golden-test, làm sau khi P0 ổn định). **CHƯA verified bằng load-test** — toàn bộ là code-evidence (file:line + DB row); sau P0+P1 cần upload 1 corpus non-commercial (legal/pharma) đo Coverage để confirm N+1-readiness.
